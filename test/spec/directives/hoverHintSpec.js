// 'use strict';

// describe('Directive hoverHint', function(){

//   var elm, target, source, $rootScope;

//   beforeEach(module('qa-rally'));

//   beforeEach(inject(function(_$rootScope_, $compile) {

//     elm = angular.element('<div id="#target"></div> <div id="source" hover-hint="{\'#target\':hintText}"></div>');
// //    target = angular.element('#target', elm);
//   //  source = angular.element('#source', elm);

//     $rootScope = _$rootScope_;
//     $compile(elm)($rootScope);
//     $rootScope.$digest();

//   }));

// //  it('is wired correctly.', function() {

// //    expect(target.length).toEqual(1);
// //    expect(source.length).toEqual(1);

// //  });
//   // TODO http://fdietz.github.io/recipes-with-angular-js/directives/testing-directives.html
//   // tests should 

// });
